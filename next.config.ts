import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // App Router i18n is handled through middleware and app directory structure
  // Remove the i18n config as it's only for Pages Router
};

export default nextConfig;
